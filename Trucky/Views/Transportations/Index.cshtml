@model IEnumerable<Trucky.Models.DB.Transportation>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DeliveryDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.StartDate)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.TruckF)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DeliveryDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TruckF.TruckId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransportationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransportationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransportationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
